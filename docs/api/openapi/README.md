# OpenAPI notes

Swagger UI and `/swagger/v1/swagger.json` are enabled for the API host.

Phase 10.1 automated check: document returns 200 and includes `/api/auth/login` without secrets.

Export a refreshed snapshot during release verification if endpoint surface changes.
