# Rendered diagrams

No PNG/SVG exports were generated in Phase 11.1.

**Reason:** Mermaid CLI (`mmdc`) / equivalent renderer was not available in the verified environment.

Use `../source/*.mmd` or the Mermaid blocks embedded via GitHub Markdown viewing. Do not claim rendered images exist until files are added here.
